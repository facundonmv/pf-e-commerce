
const initialState={
    product:[],
    category:[]

}


function rootReducer(state = initialState, action){
    switch (action.type) {
        case "GET_PRODUCT" :
            
            break;
    
        default:
            break;
    }

}

export default rootReducer;